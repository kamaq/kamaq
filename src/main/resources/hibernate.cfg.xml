<?xml version="1.0" encoding="UTF-8"?>
<hibernate-configuration>
    <session-factory>
        <!-- 
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/kamaq</property>
        <property name="connection.username">admin</property>
        <property name="connection.password">password</property>
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>
        -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/kamaq</property>
        <property name="connection.username">khipuuser</property>
        <property name="connection.password">khipu</property>        
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>
        
	<!-- JDBC connection pool (use built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- Disable the second-level cache -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

        <property name="current_session_context_class">thread</property>

	<!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

	<!-- Drop the existing table and create new one -->
        <property name="hbm2ddl.auto">create</property>  

	<!--Mention here all model classes along with their package name -->
        <mapping class ="com.raw.kamaq.model.User" />
        <mapping class ="com.raw.kamaq.model.Module" />
        <mapping class ="com.raw.kamaq.model.Component" />
        <mapping class ="com.raw.kamaq.model.Function" />
        <mapping class ="com.raw.kamaq.model.Profile" />
        <mapping class ="com.raw.kamaq.model.ProfileDetail" />

    </session-factory>
</hibernate-configuration>

